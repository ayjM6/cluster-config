apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../cluster-config
  - ../bootstrap-self
configMapGenerator:
  - name: vars
    behavior: merge
    literals:
      - type=hub
