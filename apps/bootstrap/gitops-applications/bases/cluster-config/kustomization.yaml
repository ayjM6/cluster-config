apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - appset-cluster-config.yaml
# config map used for referencing vars in replacements
configMapGenerator:
  - name: vars
    options:
      annotations:
        config.kubernetes.io/local-config: "true"
