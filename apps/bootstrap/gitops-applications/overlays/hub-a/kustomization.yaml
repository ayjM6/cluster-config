apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../bases/hub
components:
  - ../../components/namespace
  - ../../components/replace-vars
configMapGenerator:
  - name: vars
    behavior: merge
    literals:
      - name=hub-a
      - env=hub
